# 非 dotfile 文件
Makefile
README.md
CLAUDE.md
DESIGN.md
.gitignore

# 数据目录
platform
scripts

# age 加密文件（不放入 ~/）
key.txt.age
git-identity.toml.age
.password

# OS 专属配置文件，按 OS 过滤
{{ if ne .chezmoi.os "linux" }}dot_config/fontconfig{{ end }}

# remote 不使用 gpakosz/.tmux 主题的 local 配置
{{ if .remote }}dot_tmux.conf.local{{ end }}
